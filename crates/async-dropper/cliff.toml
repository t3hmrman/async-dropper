# configuration file for git-cliff (0.1.0)

[changelog]

# changelog header
header = """
# Changelog
All notable changes to this project will be documented in this file.\n
"""
# template for the changelog body
# https://tera.netlify.app/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | replace(from="v", to="") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
# remove the leading and trailing whitespaces from the template
trim = true

# changelog footer
footer = """
<!-- generated by git-cliff -->
"""

[git]
# allow only conventional commits
# https://www.conventionalcommits.org
conventional_commits = true

# regex for parsing and grouping commits
commit_parsers = [
    { message = '^feat\(dropper\)', group = "Features"},
    { message = '^fix\(dropper\)', group = "Bug Fixes"},
    { message = '^fix\(dropper\)', group = "Hotfixes"},
    { message = '^(doc|docs)\(simple\)', group = "Documentation"},
    { message = '^perf\(dropper\)', group = "Performance"},
    { message = '^pkg\(dropper\)', group = "Packaging"},
    { message = '^refactor\(dropper\)', group = "Refactor"},
    { message = '^edition\(dropper\)', group = "Editions"},
    { message = '^style\(dropper\)', group = "Styling"},
    { message = '^test\(dropper\)', group = "Testing"},
    { message = '^notes\(dropper\)', group = "Notes"},
    { message = '^workflow\(dropper\)', group = "Workflow"},
    { message = '^content\(dropper\)', group = "Content"},
    { message = '^ops\(dropper\)', group = "Operations"},
    { message = '^infra\(dropper\)', group = "Infrastructure"},
    { message = '^product\(dropper\)', group = "Product"},
    { message = '^chore\(dropper,release\): prepare for', skip = true},
    { message = '^chore\(dropper\)', group = "Miscellaneous Tasks"},
    { message = '^hide\(dropper\)', group = "Hidden", skip = true},
    { body = ".*security", group = "Security"},
]

# filter out the commits that are not matched by commit parsers
filter_commits = true

# glob pattern for matching git tags
tag_pattern = "async-dropper-v[0-9]*"

# regex for skipping tags
skip_tags = ".*beta.*"
